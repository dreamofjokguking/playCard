# Store MAP (PlayCard Bootstrap)

## 현재 상태

- `src/store` 하위 실제 스토어 구현 파일은 아직 없음.
- 기초 단계에서는 스토어 설계 인덱스만 유지.

## 추가 예정 스토어

- `bgmStore.ts`
- `boardStore.ts`
- `wbsStore.ts`
- `modalStore.ts`

## 공통 규칙

- 파일명: camelCase + `Store` 접미어
- `zustand` + `devtools` 미들웨어 사용
