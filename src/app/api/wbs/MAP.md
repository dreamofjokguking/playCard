# WBS API MAP (Bootstrap)

## 현재 상태

- 현재 `src/app/api/wbs` 하위 실제 Route Handler는 아직 없음.

## 추가 예정 라우트

- `tasks/route.ts` (목록/생성)
- `tasks/[taskId]/route.ts` (단건 조회/수정/삭제)
- `tasks/batch/route.ts` (일괄 수정)

## 연관 경로

- 스토어: `src/store/wbsStore.ts` (예정)
- 컴포넌트: `src/components/wbs/` (예정)
