# AI API MAP (Bootstrap)

## 현재 상태

- 현재 `src/app/api/ai` 하위 실제 Route Handler는 아직 없음.
- 본 문서는 기획서 기준 예정 경로 인덱스만 관리함.

## 추가 예정 라우트

- `generate-instruction/route.ts`
- `execution-result/route.ts`
- `presets/route.ts`
- `history/[id]/route.ts`

## 의존 예정 모듈

- `src/lib/gemini.ts`
- `src/lib/models/*` (AI 관련 이력/설정 모델)
