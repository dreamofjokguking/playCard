## 2026-05-04 — kimis (feature/PC-2-dashboard-chart-rank-delta-absence-auto) `7d7f8c7`

> 모델: claude (spm-done 자동생성)

## 작업 요약

PC-2 시각화 통합 작업. 디자인 시안의 RadarChart/LineChart를 SVG 그대로 React 컴포넌트로 옮겨 대시보드를 차트 기반으로 재구성하고, 순위 페이지에 직전 경기 대비 ▲▼− 변동 뱃지를 추가했다. 평가 결과 집계 시 클럽룸의 활성 메트릭과 본인 포지션 선언을 비교하여 결장(absences)을 자동 산출하도록 보강(PC-1 후속). `recharts` 등 차트 라이브러리는 도입하지 않고 `colors_and_type.css` 토큰만으로 구현. 검수 편의를 위해 `/dashboard/preview`, `/ranking/preview` mock 라우트와 `/dev` 페이지(시드 트리거 + 빠른 사용자 전환)를 추가했고, dev 전용 시드 API(`/api/dev/seed`)로 ClubRoom/User/Match 더미 데이터를 한 번에 생성할 수 있게 했다.

## 변경된 파일

- `src/components/ui/chartUtils.ts` — 신규. radarPolygon / lineChartGeometry / pickBestWorst / rankDelta 등 차트·순위 산출 helper 분리
- `src/components/ui/chartUtils.test.ts` — 신규. 13건 단위 테스트
- `src/components/ui/RadarChart.tsx` — 신규. SVG RadarChart + BEST(★ 골드)/NEED(▼ 블루) 라벨
- `src/components/ui/LineChart.tsx` — 신규. SVG LineChart + 그라디언트 영역 + 마지막 점 값 라벨
- `src/components/ui/RankRow.tsx` — `previousRank` prop 추가, ▲▼− 뱃지 렌더
- `src/app/api/rankings/route.ts` — accumulate / rankByTotals 분리, 최신 1경기 제외 누적으로 previousRank 산출 → 응답에 포함
- `src/app/api/rankings/route.test.ts` — `previousRank` 산출 검증 케이스 1건 추가
- `src/app/api/evaluations/route.ts` — aggregateResults 시그니처에 AggregationContext 추가, 호출부에서 ClubRoom 조회 + positionSubmissions 매핑 전달, 미선언 메트릭은 자동으로 absences에 반영
- `src/app/api/evaluations/route.test.ts` — ClubRoom mock + 자동 absences 산출 검증 추가
- `src/app/dashboard/page.tsx` — 능력치 카드 → RadarChart, "성장 그래프" 카드 신설(LineChart), 메트릭 한국어 라벨 매핑
- `src/app/ranking/page.tsx` — RankingRow 타입에 `previousRank` 추가, RankRow에 전달
- `src/app/api/dev/seed/route.ts` — 신규. dev 전용 시드 API. ClubRoom 1개 + User 8명 + Match 5경기 생성, 자동 로그인 쿠키 세팅
- `src/app/dev/page.tsx` — 신규. 시드 트리거 + 사용자 전환 + 페이지 바로가기 도구 페이지
- `src/app/dashboard/preview/page.tsx` — 신규. mock 데이터로 5경기/1경기/empty 케이스 차트 검수
- `src/app/ranking/preview/page.tsx` — 신규. mock 데이터로 ▲▼−/신규 진입 케이스 검수
- `src/app/api/MAP.md` — `/api/dev/seed` 엔드포인트 반영

## 테스트 결과

- 실행 명령: `npm run test:run`
- 결과: 72 passed / 0 failed (이전 58 → 72, +14건)
- 실행 명령: `npx tsc --noEmit`
- 결과: 통과
- 신규 추가 테스트: 1개 파일 (`src/components/ui/chartUtils.test.ts`, 13건) + 기존 2개 파일에 케이스 추가 (`rankings/route.test.ts` +1, `evaluations/route.test.ts` 보강)
- 미작성 테스트 및 사유:
  - RadarChart/LineChart 자체의 React 렌더 테스트 — vitest 환경이 node + `@testing-library/react` 미도입. SVG 좌표 계산은 chartUtils로 분리해 단위 테스트로 커버됨.
  - `/api/dev/seed`는 실 mongoose 모델을 직접 사용하는 dev 전용 시드 라우트라 vitest 통합 테스트 대신 dev 서버에서 수동 호출로 검증.

## 건드리면 안 되는 부분

| 파일 | 위치 | 이유 |
| ---- | ---- | ---- |
| `src/app/api/evaluations/route.ts` | `aggregateResults` 시그니처 + 호출부(ClubRoom 조회 + positionSubmissions 전달) | 결과 집계의 핵심 계약. context 필드 이름/타입 변경 시 결장 자동 산출이 조용히 깨짐 |
| `src/app/api/rankings/route.ts` | `accumulate` / `rankByTotals` 분리 및 `previousTotals` 계산 | 직전 순위 산출은 "최신 1경기 제외 누적"이라는 전제에 맞춰져 있음. matches 정렬이나 `slice(1)` 변경 시 의미가 뒤틀림 |
| `src/components/ui/chartUtils.ts` | `radarPolygon` / `lineChartGeometry` / `rankDelta` | UI/SVG/순위 산출의 단일 진실원. 시그니처 변경 시 RadarChart/LineChart/RankRow가 동시 깨짐 |
| `src/app/api/dev/seed/route.ts` | `NODE_ENV` 가드 + 기존 시드 정리 로직 | dev 전용 시드 API. 가드 제거 시 운영 DB도 더미로 오염될 수 있음 |

## 미완성 / 다음 세션에서 이어받을 부분

- 기획서 P3 — AI 칭호(Gemini) 미착수. 평가 마감 훅에 연결하면 `User.currentTitle`/`titleHistory`가 자동 채워짐(모델 필드는 이미 존재).
- 기획서 P4 — PWA(`manifest.json` + service worker) 미착수.
- 기획서 P5 — Kakao OAuth 전환. 운영 진입 전 필수.
- 시드 데이터는 결장 없이 풀세트로 생성됨. PC-1 흐름(본인 포지션 선언) 기반 결장 자동 산출 로직은 코드와 단위 테스트로 검증되어 있으며, 실 DB에서도 사용자가 일부 포지션을 미선언하면 자동으로 결장 처리됨.
