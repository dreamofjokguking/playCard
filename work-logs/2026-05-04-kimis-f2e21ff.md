## 2026-05-04 — kimis (fix/PC-7-design-system-audit) `f2e21ff`

> 모델: claude (spm-done 자동생성)

## 작업 요약

`docs/design/colors_and_type.css` 시안과 `src/app/globals.css` 코드의 격차를 전반 점검해 audit 보고서(`docs/plans/design-audit.md`)를 작성하고, 즉시 보강 가능한 부분을 본 PR에 포함했다. 시안 토큰을 globals.css에 alias·신규로 모두 추가해 다음 PR부터 점진 마이그레이션이 가능하도록 기반을 만들고, podium 색·RankRow 등수·버튼 transition·bottom-nav active tint 등 시안과 가장 어긋났던 즉각 시각 격차를 토큰 기반으로 보정했다. 큰 변경(등급별 그라디언트 PlayerCard, 카드형 BottomNav)은 후속 PR로 분리.

## 변경된 파일

- `docs/plans/design-audit.md` (신규) — 시안 vs 코드 토큰 매핑 표, 컴포넌트·카피 톤 격차, 본 PR 처리 항목/후속 PR 분리
- `src/app/globals.css`
  - 신규 토큰: 50+ 종 (surface/border/ink/brand-variant/semantic/rarity/team/trend/podium/typo-scale/spacing/radii/shadow-glow/easing/duration/z-index)
  - alias: `--pc-border` ← line, `--pc-ink` ← text, `--pc-ink-secondary` ← muted, `--pc-primary-tint` ← soft, `--pc-primary-hover` ← strong
  - JetBrains Mono import
  - 시맨틱 typography 유틸 클래스(`.pc-display`/`.pc-h1~3`/`.pc-body`/`.pc-caption`/`.pc-mono`/`.pc-score`/`.pc-stat-num`/`.pc-glow-*`)
  - rarity chip 유틸(`.pc-rarity-chip` + `.pc-rarity-{common,rare,epic,legendary}`, legendary는 그라디언트 + 골드 글로우)
  - `.pc-button` transition(ease-bounce 200ms) + active scale(0.97) + primary는 `box-shadow: 0 2px 0 press` + amber glow + hover 색
  - `.pc-bottom-link.is-active .pc-nav-icon` tint 배경
- `src/components/ui/RankRow.tsx` — RANK_NUMBER_COLOR(podium 토큰) + isPodium 분기 + 등수 숫자 italic 900 tnum
- `src/app/club-rooms/[id]/page.tsx` — podium 그라디언트 인라인 hex → 토큰
- `src/app/club-rooms/[id]/ranking/page.tsx` — 동일

## 테스트 결과

- 실행 명령: `npm run test:run`
- 결과: 86 passed / 0 failed (변동 없음)
- 실행 명령: `npx tsc --noEmit`
- 결과: 통과
- 실행 명령: `npm run build`
- 결과: 통과 (clean rebuild 후) — 라우트 변동 없음, CSS만 변경
- 신규 추가 테스트: 없음 (시각 토큰·유틸 클래스 추가는 단위 테스트 영역이 아님)
- 미작성 테스트 및 사유: 시각 회귀 검증은 Playwright/Storybook 등 별도 환경 필요. 본 PR 범위 외.

## 건드리면 안 되는 부분

| 파일 | 위치 | 이유 |
| ---- | ---- | ---- |
| `src/app/globals.css` | `:root` 토큰 블록 + alias 관계 (`--pc-text`, `--pc-line`, `--pc-muted`, `--pc-primary-soft`, `--pc-primary-strong`) | 시안 이름·기존 이름을 동시 제공하는 alias 구조. 기존 alias를 제거하면 다수 컴포넌트가 대규모로 깨짐. 새 이름으로의 마이그레이션은 페이지 단위로 점진 진행 후 마지막에 alias 삭제 |
| `src/app/globals.css` | `.pc-rarity-*` / `.pc-display`/`.pc-h*`/`.pc-score` 등 유틸 클래스 | 다음 PR부터 점진 적용 기반. 이름/스펙 변경 시 추후 적용 코드 깨짐 |
| `src/components/ui/RankRow.tsx` | `RANK_NUMBER_COLOR` 매핑 + `isPodium` 분기 | 시안의 podium 색 철칙. 1/2/3 분기가 깨지면 순위 표의 시각적 계층 소실 |
| `docs/plans/design-audit.md` | 토큰 매핑 표 + 컴포넌트 격차 표 | 다음 PR 범위 결정 시 참조 자료. 임의 수정 시 이후 작업 방향이 흐릿해짐 |

## 미완성 / 다음 세션에서 이어받을 부분

- 등급별 그라디언트 PlayerCard 컴포넌트 신규 — `docs/design/preview/components-playercard.html` 시안 그대로 (common 회색, epic 보라, legend 골드 그라디언트). 클럽 메인/도감/순위 등에 일관 적용
- 모바일 BottomNav 카드형 — width 380px, padding 8px, radius 20px (시안 정확 재현)
- 인라인 hex/px → 토큰 사용으로 페이지 단위 점진 마이그레이션 (audit 보고서의 후속 PR 분리 항목 참조)
- PWA install prompt 검증 — `npm run build && npm start` 프로덕션 모드에서 Chrome 주소창 install 아이콘 노출 확인. 미노출 시 PNG 192/512 raster icon 보강 (PC-6 work-log 후속 항목)
- 후속 우선순위(`docs/plans/spm-priorities.md`): P5 Kakao OAuth
