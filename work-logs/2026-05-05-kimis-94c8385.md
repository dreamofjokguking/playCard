## 2026-05-05 — kimis (feature/PC-12-notifications-redesign) `94c8385`

> 모델: claude (spm-done 자동생성)

## 작업 요약

알림함이 "읽음/새 알림" 텍스트 라벨 + 풀데이트 타임스탬프만 보이는 1차원 리스트라 어떤 알림인지 한눈에 들어오지 않던 문제를 해결. 알림 type별 아이콘/색 매핑(`evaluation.position.requested`→클립보드/primary, `evaluation.started`→휘슬/secondary, `evaluation.completed`→트로피/accent, fallback→벨), 상대 시간 helper(방금/N분전/N시간전/어제/N일전/MM-DD), 좌측 원형 tint 아이콘 + 우측 본문 + 우상단 미확인 dot 카드 레이아웃, 빈 상태 점선 카드(필터별 안내 분기), 헤더 "전체 읽음" 버튼이 미확인 0이면 disabled 되도록 정비. 모두 디자인 토큰(`--pc-primary-tint`/`--pc-secondary-tint`/`--pc-accent-tint`/`--pc-surface-elevated`/`--pc-ink-tertiary`) 기반.

## 변경된 파일

- `src/app/club-rooms/[id]/notifications/page.tsx` — 카드 레이아웃 재구성, `metaForType()`/`formatRelative()` helper, `NotificationIcon` 컴포넌트(휘슬/깃발/트로피/클립보드/벨), 빈 상태 분기, 헤더 미확인 카운터
- `src/app/globals.css` — `.pc-notification-section/header/list/item/icon/body/row/title/time/message/dot/empty` + `.pc-tone-{primary,secondary,accent}` 신규 클래스. 기존 `.pc-notification-item.is-unread` hover 토큰화
- `CLAUDE.md` — 진행 현황 표 PC-12 행 → 기본 행 복원 (10단계에서)

## 테스트 결과

- 실행 명령: `npm run test:run`
- 결과: 86 passed / 0 failed (변동 없음)
- 실행 명령: `npx tsc --noEmit`
- 결과: 통과
- 실행 명령: `npm run build`
- 결과: 통과 (clean rebuild, `/club-rooms/[id]/notifications` 2.42 kB)
- 신규 추가 테스트: 없음 (presentational redesign — 기존 데이터 흐름/계약 무변경)
- 미작성 테스트 및 사유: `formatRelative()`는 순수 함수라 단위 테스트 가능하나 시간 모킹 필요. 차후 `chartUtils.test.ts`처럼 별도 유틸 파일로 분리 시 함께 테스트 추가 예정.

## 건드리면 안 되는 부분

| 파일 | 위치 | 이유 |
| ---- | ---- | ---- |
| `src/app/club-rooms/[id]/notifications/page.tsx` | `connectSocket()` + `socket.on('notification-created', ...)` + cleanup `socket.disconnect()` | 실시간 알림 수신 핵심 로직. cleanup 누락 시 페이지 이탈 후에도 좀비 소켓이 남고, listener 등록이 빠지면 신규 알림 푸시 시 자동 갱신 불능 |
| `src/app/club-rooms/[id]/notifications/page.tsx` | `metaForType()` 매핑 키 (`evaluation.position.requested`/`evaluation.started`/`evaluation.completed`) | `src/app/api/evaluations/route.ts:349`, `src/app/api/evaluations/positions/route.ts:71`, `src/app/api/admin/matches/route.ts:115`의 type 문자열과 1:1 대응. 서버 측 type 문자열 변경 시 여기도 같이 수정해야 fallback(`bell/neutral`)으로 빠지지 않음 |
| `src/app/club-rooms/[id]/notifications/page.tsx` | `openNotification()` — read PATCH 후 `load()` 호출 순서 | 읽음 처리 → 재조회 순서가 뒤바뀌면 dot이 즉시 사라지지 않거나 stale 데이터로 라우팅됨 |

## 미완성 / 다음 세션에서 이어받을 부분

- `formatRelative()` 단위 테스트 (시간 모킹 필요 — `vi.useFakeTimers()` 패턴)
- Notification type 신규 추가 시 `metaForType()` 동기화를 잊지 않도록 모델 enum 또는 공유 상수화 검토
- 후속 우선순위(`docs/plans/spm-priorities.md`): P5 Kakao OAuth (외부 Kakao Developers 등록 필요), PNG 192/512 raster icon 보강, PWA install button 운영 환경 검증
