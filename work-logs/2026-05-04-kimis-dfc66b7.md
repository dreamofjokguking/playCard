## 2026-05-04 — kimis (fix/PC-5-title-majority-threshold) `dfc66b7`

> 모델: claude (spm-done 자동생성)

## 작업 요약

PC-4 AI 칭호 시스템에 신뢰성·전설 등급 정책을 보강하고, `docs/etc/` 사진 양식을 기준으로 결과 페이지를 메트릭별 랭킹 표로 재구성했다. 사용자×메트릭 시계열 히스토리 페이지를 신설하고, 순위 페이지의 매치 셀렉트박스 기본값을 가장 최근 매치로 바꿔 한 화면에서 그날 결과가 즉시 보이게 했다.

## 변경된 파일

- `src/app/api/evaluations/route.ts`
  - 칭호 생성 가드: `Math.ceil(evaluatorCount * 0.5)` 미만 평가받은 선수는 칭호 스킵
  - legendary 정책: 만장일치 MVP(`mvpCount === evaluatorCount`)만 legendary, AI가 legendary 응답해도 비-만장일치면 epic으로 강등
- `src/app/api/evaluations/route.test.ts` — 과반 미만 스킵 시나리오 1건 추가, 기존 기대값 갱신
- `src/app/api/matches/[id]/results/route.ts` — 응답에 `isUnanimousMvp`, `evaluatorCount`, `playerStats[].title/rarity` 추가
- `src/app/club-rooms/[id]/evaluation/[matchId]/result/page.tsx` — 사진 양식대로 재작성: 매치 헤더 + MVP 카드(만장일치 글로우) + 오늘의 칭호 그리드 + 메트릭별 랭킹 표(동률 같은 순위, 결장 별도 처리) + 한줄평 섹션
- `src/app/club-rooms/[id]/ranking/page.tsx` — 매치 셀렉트박스 기본값을 가장 최근 매치로 (시즌 누적 명시 선택은 보존)
- `src/app/api/club-rooms/[id]/history/route.ts` — 신규. 사용자×메트릭 매트릭스 + 직전 출전 매치 대비 delta 계산
- `src/app/club-rooms/[id]/history/page.tsx` — 신규. sticky 첫 컬럼 가로 스크롤 표(평균 / 증감 ▲ 빨강 ▼ 파랑), 참여자 필터
- `src/app/club-rooms/[id]/page.tsx` — player-card에 "히스토리" 진입점 추가
- `src/app/api/MAP.md` — `/api/club-rooms/[id]/history` 라우트 반영
- `docs/etc/` — 양식 참조 자료(사진/오디오) 커밋

## 테스트 결과

- 실행 명령: `npm run test:run`
- 결과: 86 passed / 0 failed (이전 85 → +1)
- 실행 명령: `npx tsc --noEmit`
- 결과: 통과
- 실행 명령: `npm run build`
- 결과: 통과 (clean rebuild로 캐시 정리 후) — `/club-rooms/[id]/history` 정상 등록
- 신규 추가 테스트: 1건 (`evaluations/route.test.ts` "skips title generation for players whose ratedBy < majority threshold")
- 미작성 테스트 및 사유:
  - 만장일치 MVP legendary 강제/강등 분기는 단위 테스트 미작성. 평가 마감 훅의 mock 시나리오를 대형으로 늘려야 해 본 PR에선 스킵 — 후속에서 보강 검토.
  - `/api/club-rooms/[id]/history`, `/api/matches/[id]/results` 응답 변경 부분도 단위 테스트 미작성. 클라이언트 화면이 그 자체로 통합 검증 역할.

## 건드리면 안 되는 부분

| 파일 | 위치 | 이유 |
| ---- | ---- | ---- |
| `src/app/api/evaluations/route.ts` | `titleThreshold`, `unanimousMvpUserId`, `finalRarity` 분기 | 칭호 생성·등급 정책의 단일 진실원. 임계 계산(`Math.ceil(eval*0.5)`) 또는 legendary 강등/강제 분기 변경 시 사용자 경험 조용히 일그러짐 |
| `src/app/api/club-rooms/[id]/history/route.ts` | 사용자×메트릭 매트릭스 조립 + `lastAvg` 기반 delta 계산 | 히스토리 표의 단일 데이터 소스. delta 계산은 "출전한 직전 매치 기준"이라 결장 스킵 로직 변경 시 증감 잘못 표시 |
| `src/app/club-rooms/[id]/evaluation/[matchId]/result/page.tsx` | `rankingsByMetric` (동률 같은 순위 산출) | 사진 양식 재현 핵심. 결장 분류·동률 처리 변경 시 절대/상대 순위가 뒤섞임 |
| `src/app/club-rooms/[id]/ranking/page.tsx` | matchId 초기값 설정 (`prev === SEASON_ALL ? list[0]._id : prev`) | 사용자가 시즌 누적을 명시적으로 선택하면 그게 유지되도록 하는 가드. 제거 시 매치 목록 재로드마다 선택 강제 돌림 |

## 미완성 / 다음 세션에서 이어받을 부분

- 만장일치 MVP legendary 강제/강등 분기 단위 테스트 추가 (회귀 검증용)
- 히스토리 표 컬럼 폭이 매치 수 많아지면 가로 스크롤 부담. 후속 PR에서 페이지네이션 또는 최근 N매치 토글 검토
- 결과 페이지의 한줄평 섹션을 "내 평가 / 받은 평가" 두 시점으로 분리 검토 (현재는 사용자별 받은 평가만)
- 후속 우선순위(`docs/plans/spm-priorities.md`): P4 PWA, P5 Kakao OAuth
