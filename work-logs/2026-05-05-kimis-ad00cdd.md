## 2026-05-05 — kimis (feature/PC-8-player-card-component) `ad00cdd`

> 모델: claude (spm-done 자동생성)

## 작업 요약

PC-7 audit에서 후속 PR로 분리했던 등급별 그라디언트 PlayerCard 컴포넌트를 시안(`docs/design/preview/components-playercard.html`) 그대로 신설. 클럽 메인과 칭호 도감 페이지에 적용해 등급(common/rare/epic/legendary)에 따른 시각적 차별화를 강화. dashboard API 응답에 `currentRarity` 필드를 노출해 클라이언트에서 등급 prop으로 전달.

## 변경된 파일

- `src/components/ui/PlayerCard.tsx` (신규) — 등급별 그라디언트, LV/RARITY chip, 큰 원형 아바타, 칭호 chip, `variant: 'compact' | 'full'` 분기, `trailing` prop, legendary 글로우
- `src/app/api/users/[id]/dashboard/route.ts` — User select에 `currentRarity` 추가, 응답 user 객체에 노출
- `src/app/club-rooms/[id]/page.tsx` — 기존 `pc-player-card` 영역을 `<PlayerCard variant="compact" />`로 교체. EXP 진행률 + 도감/히스토리 진입점은 별도 카드로 분리
- `src/app/club-rooms/[id]/titles/page.tsx` — "현재 칭호" 카드를 `<PlayerCard variant="compact" />`로 교체. 칭호 없을 때 안내는 그대로 유지

## 테스트 결과

- 실행 명령: `npm run test:run`
- 결과: 86 passed / 0 failed (변동 없음)
- 실행 명령: `npx tsc --noEmit`
- 결과: 통과
- 실행 명령: `npm run build`
- 결과: 통과 (clean rebuild 후) — 라우트 변동 없음, UI 컴포넌트 추가만
- 신규 추가 테스트: 없음 (시각 컴포넌트 단위 테스트 인프라 부재 — 빌드 + 수동 검증으로 갈음)
- 미작성 테스트 및 사유: PlayerCard React 렌더 테스트는 `@testing-library/react` 미도입 환경이라 미작성. 그라디언트/조건분기 정도라 후속 PR에서 도구 도입 시 함께 보강 권장.

## 건드리면 안 되는 부분

| 파일 | 위치 | 이유 |
| ---- | ---- | ---- |
| `src/components/ui/PlayerCard.tsx` | `RARITY_GRADIENT` 매핑 | 디자인 시안의 등급별 그라디언트 철칙. 임의 수정은 등급 시각 계층을 깨뜨림 |
| `src/components/ui/PlayerCard.tsx` | `variant: compact / full` 조건 분기 | 클럽 메인(compact) / 도감(compact) / 향후 컬렉션(full) 레이아웃 계약. 변경 시 적용처 헤더 영역이 키 계산과 어긋남 |
| `src/app/api/users/[id]/dashboard/route.ts` | `currentRarity` select 필드 | 클라이언트가 PlayerCard rarity prop으로 이 값을 넘김. select에서 빠지면 항상 common으로 떨어져 디자인이 단조로움 |
| `src/app/club-rooms/[id]/page.tsx` | `latest`, `expPercent`, 도감·히스토리 진입 배치 | PlayerCard와 EXP 카드를 분리한 레이아웃. 재합치면 이동 동선이 다시 불명해짐 |

## 미완성 / 다음 세션에서 이어받을 부분

- 결과 화면(`/club-rooms/[id]/evaluation/[matchId]/result`)의 "오늘의 칭호" 그리드를 미니 PlayerCard로 교체 검토 — 현재는 chip 형태. 후속 시각 통일 PR.
- 시즌 podium에 PlayerCard 사용 검토 (현재는 카드 그라디언트 + 막대만)
- BottomNav 카드형 (PC-7 후속 분리 항목)
- 후속 우선순위(`docs/plans/spm-priorities.md`): P5 Kakao OAuth (외부 등록 필요), PWA install prompt 검증
