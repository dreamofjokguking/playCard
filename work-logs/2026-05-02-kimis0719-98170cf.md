## 2026-05-02 — kimis0719 (feature/SPM-4-github-ops-hardening) `98170cf`

> 모델: codex (spm-done 자동생성)

## 작업 요약

타 프로젝트에서 가져온 `.github` 설정을 playCard 구조로 정리하고, GH 토큰/프록시 문제 재발 방지 가드와 가이드를 추가했다.

## 변경된 파일

- `.github/workflows/ci.yml` — playCard 스크립트 기준 CI(test/typecheck/build)로 교체
- `.github/workflows/auto-worklog.yml` — 누락 스크립트 감지 후 안전 스킵하도록 정리
- `.github/workflows/conflict-guard.yml` — `github-script` 기반 충돌 코멘트 방식으로 교체
- `.github/workflows/preview-deploy.yml` — 하드코딩 URL 제거, `PREVIEW_URL` 변수 기반 안내로 변경
- `.github/workflows/update-maps.yml` — 누락 스크립트 감지 후 안전 스킵하도록 정리
- `.github/copilot-instructions.md` — 프로젝트명을 PlayCard 기준으로 재작성
- `.github/pull_request_template.md` — SPM/테스트/Linear 연동 템플릿으로 교체
- `scripts/gh-with-env.ps1` — `.env.local` `GH_TOKEN` 우선 로딩 + 깨진 프록시 자동 해제 래퍼 추가
- `docs/gh-auth-troubleshooting.md` — GH 인증 장애 원인/복구 절차 문서 추가
- `.workzones.yml` — SPM-4 작업 구역 해제
- `CLAUDE.md` — 현재 작업 현황 표 복원

## 테스트 결과

- 실행 명령: `npm run test:run`
- 결과: 11 passed / 0 failed
- 신규 추가 테스트: 0개
- 실행 명령: `npx tsc --noEmit`
- 결과: 통과
- 미작성 테스트 및 사유: 워크플로우/문서/스크립트 운영 정비 작업으로 애플리케이션 런타임 로직 변경 없음

## 건드리면 안 되는 부분

| 파일 | 위치 | 이유 |
| --- | --- | --- |
| `scripts/gh-with-env.ps1` | proxy 해제 + token 주입 로직 | GH 인증 실패 재발 방지 핵심 경로로, 임의 변경 시 동일 장애 재발 가능 |
| `.github/workflows/conflict-guard.yml` | `Detect overlaps and comment` step | PR 자동 코멘트 가드의 기준 로직이라 문법/권한 변경 시 워크존 경고 누락 위험 |

## 미완성 / 다음 세션에서 이어받을 부분

- `preview-deploy.yml`의 `PREVIEW_URL` Repository Variable 운영 환경값 점검 필요
