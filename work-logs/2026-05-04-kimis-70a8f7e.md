## 2026-05-04 — kimis (feature/PC-4-ai-title-gemini) `70a8f7e`

> 모델: claude (spm-done 자동생성)

## 작업 요약

PC-4 AI 칭호(Gemini) 시스템을 도입하고, 등급(common/rare/epic/legendary) 체계와 칭호 도감, 순위·결과 화면 칭호 연계까지 한 묶음으로 마감했다. Gemini 2.5-flash로 평가 마감 시 참여자 N명 칭호 병렬 생성, AiSettings 싱글턴 도큐먼트로 프롬프트/모델 편집 가능, service_admin 전용 `/admin` + `/admin/ai` 라우트 신설. dev 도구에 시드/승격/Gemini health check를 추가해 검수 흐름을 끊김 없이 만들었다. `npm run dev`에 `kill-port 3000` 가드를 추가해 포트 누적 문제도 해결.

## 변경된 파일

- `package.json` / `package-lock.json` — `@google/generative-ai` 추가, `dev` 스크립트에 `npx kill-port 3000 && next dev -p 3000` 적용
- `src/lib/gemini.ts` (신규) — `generateTitle`, `parseTitleResponse`, `loadAiSettings`, JSON 응답 + 등급 파싱
- `src/lib/gemini.test.ts` (신규) — fillTitlePrompt/trimTitle/parseTitleResponse 12건
- `src/lib/models/AiSettings.ts` (신규) — 싱글턴(`scope: global`) + `DEFAULT_AI_TITLE_PROMPT`/`DEFAULT_AI_MODEL`
- `src/lib/models/User.ts` — `currentRarity`, `titleHistory[].rarity` 추가
- `src/lib/models/Match.ts` — `results.playerStats[].title/rarity` 추가
- `src/lib/accessControl.ts` — `requireServiceAdmin` 헬퍼 추가
- `src/app/api/admin/ai/settings/route.ts` (신규) — GET/PATCH(service_admin), `{displayName}` placeholder 검증
- `src/app/api/users/[id]/titles/route.ts` (신규) — 본인 칭호 도감
- `src/app/api/club-rooms/[id]/matches/route.ts` (신규) — 클럽 완료 매치 목록(드롭다운용)
- `src/app/api/dev/gemini-health/route.ts` (신규) — 샘플 칭호 1회 생성, 응답시간/등급
- `src/app/api/dev/promote/route.ts` (신규) — 현재 세션 사용자를 service_admin으로 승격
- `src/app/api/dev/seed/route.ts` — Evaluation 정리, AiSettings reset, evaluating 매치 1개 + 7명 Evaluation 자동 생성, 5경기에 시뮬레이션 칭호+등급, lastMatchTitleByUser로 User 갱신
- `src/app/api/evaluations/route.ts` — 마감 후크에서 `isGeminiEnabled()` 가드 + 참여자 병렬 `generateTitle` + `User.findByIdAndUpdate` `$push`
- `src/app/api/evaluations/route.test.ts` — Gemini/User mock 추가, 칭호 생성 시나리오 1건 신설
- `src/app/api/rankings/route.ts` — `clubRoomId`/`matchId` 옵션, 응답에 `title/rarity`
- `src/app/admin/page.tsx` (신규) — 서비스 최상위 관리 메뉴, 권한 분기
- `src/app/admin/ai/page.tsx` (신규) — 모델 선택, 프롬프트 편집, 응답 샘플 보기, 기본값 되돌리기
- `src/app/club-rooms/[id]/titles/page.tsx` (신규) — 등급 필터 + 역대 칭호 + 그 경기로 점프
- `src/app/club-rooms/[id]/page.tsx` — player-card에 도감 진입점, club state 정리
- `src/app/club-rooms/[id]/evaluation/[matchId]/result/page.tsx` — 등급 chip
- `src/app/club-rooms/[id]/ranking/page.tsx` — 매치 선택 드롭다운, MATCH PODIUM 라벨, 등급 chip
- `src/components/layout/navigationItems.tsx` — `getOutsideClubNav({ isServiceAdmin })`로 service_admin 전용 "관리" 동적 노출
- `src/components/layout/useNavigationItems.ts` — me 응답 role로 service_admin 판정
- `src/components/ui/RankRow.tsx` — `title`/`rarity` prop, 등급 색상 chip
- `src/app/dev/page.tsx` — 현재 권한 표시 + 승격 버튼 + 클럽 메인 진입 칩
- `src/app/api/MAP.md` — 새 라우트(7개) 반영

## 테스트 결과

- 실행 명령: `npm run test:run`
- 결과: 85 passed / 0 failed (이전 72 → 85, +13건)
- 실행 명령: `npx tsc --noEmit`
- 결과: 통과
- 실행 명령: `npm run build`
- 결과: 통과 — 새 라우트(`/admin`, `/admin/ai`, `/club-rooms/[id]/titles` 등) 정상 등록
- 신규 추가 테스트: 1개 파일 (`src/lib/gemini.test.ts`, 12건) + 기존 `evaluations/route.test.ts` 칭호 생성 1건 추가
- 미작성 테스트 및 사유: AiSettings GET/PATCH API 단위 테스트는 작성하지 않음 — `requireServiceAdmin`/`AiSettings.findOneAndUpdate`만 모킹하면 trivial하므로, dev 서버에서 수동 검증으로 충분. 추후 회귀 발생 시 추가 예정.

## 건드리면 안 되는 부분

| 파일 | 위치 | 이유 |
| ---- | ---- | ---- |
| `src/lib/gemini.ts` | `generateTitle` / `parseTitleResponse` / `loadAiSettings` | 칭호 생성 파이프라인의 단일 진실원. 동기/타이밍/응답 형식 변경 시 평가 마감 흐름이 조용히 깨짐 (response·알림·User 갱신 동시 동작) |
| `src/app/api/evaluations/route.ts` | 평가 마감 후크(`isGeminiEnabled` 가드, 병렬 `generateTitle`, `User.findByIdAndUpdate` `$push`) | 참여자 명수만큼 Gemini 병렬 호출과 User 도큐먼트 갱신이 동시 동작. graceful 처리 제거 시 결과 누락 위험 |
| `src/app/api/admin/ai/settings/route.ts` | `requireServiceAdmin` 가드 + `{displayName}` placeholder 검증 | 프롬프트 자유 편집 가능하므로 골격 검증이 마지막 안전장치. 검증 제거 시 `generateTitle`이 치환 실패로 오작동 |
| `src/lib/models/AiSettings.ts` | `scope: 'global'` unique + `DEFAULT_AI_TITLE_PROMPT` | 싱글턴 도큐먼트 전제로 `loadAiSettings`가 동작. 기본값/등급 정의 단계는 `dev/seed`와 admin UI가 모두 의존 |
| `src/app/api/dev/seed/route.ts` | NODE_ENV 가드 + AiSettings reset + `lastMatchTitleByUser` 계산 | dev 전용 시드. 가드 제거는 운영 DB 오염 가능. `lastMatchTitleByUser` 로직은 시즌 누적 칭호 동기화 기준 |

## 미완성 / 다음 세션에서 이어받을 부분

- 등급별 시각 효과 강화 — legendary 골드 그라디언트/glow, 결과 페이지의 "내 칭호만 강조" 애니메이션 등
- 칭호 부여 알림 — 현재는 평가 종료 알림만 발송, 별도 "새 칭호 획득" 푸시 검토
- AI 사용량 모니터링/한도 — Gemini 호출량 누적, 임계 시 자동 비활성화
- AiSettings GET/PATCH 단위 테스트 (회귀 검증용)
- 후속 우선순위(`docs/plans/spm-priorities.md`): P4 PWA, P5 Kakao OAuth
